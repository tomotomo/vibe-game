namespace Daifugo.Core
{
    public enum Suit
    {
        Spades,
        Hearts,
        Diamonds,
        Clubs,
        None // For invalid or special cases if needed, though mostly unused in this spec
    }
}
